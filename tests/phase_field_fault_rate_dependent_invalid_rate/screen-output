
Loading shared library <./libphase_field_fault_rate_dependent_invalid_rate.debug.so>

TimerOutput objects finalize timed values printed to the
screen by communicating over MPI in their destructors.
Since an exception is currently uncaught, this
synchronization (and subsequent output) will be skipped
to avoid a possible deadlock.


Exception 'ExcMessage("Characteristic weakening slip rates must be finite and positive.")' on rank 0 on processing:

An error occurred in file <fault_friction.cc> in function
(line in output replaced by default.sh script)
The violated condition was:
    numbers::is_finite(characteristic_weakening_slip_rates[j]) && characteristic_weakening_slip_rates[j] > 0.0
Additional information:
    Characteristic weakening slip rates must be finite and positive.

Stacktrace:
(rest of the output replaced by default.sh script)

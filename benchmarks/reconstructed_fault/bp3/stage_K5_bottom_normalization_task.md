# K5: complete the bottom normalization — bounded diagnostic

Test whether boundary truncation of I_h causes the RSF-independent bottom hotspot. Reuse `stage_K5_uniform_sliding_report.md` and its existing uniform-sliding baseline. Implement one opt-in diagnostic; keep the ordinary normalization policy unchanged pending the result.

1. **Define the completion in the actual assembly.** Treat the bottom intersection as a straight fault continuing outside the box. Construct a complete transverse normalization by adding the missing outside-profile contribution before the final I_h projection. Derive this from the existing weak normalization definition, including its weights; do not merely double the endpoint coefficient or add a pointwise correction to an already projected field.

   In the report's coordinates, n_y=1/2: at fault height y, the missing normal-coordinate interval is r<-2y. With profile radius approximately 790.6 m, geometric clipping affects heights below approximately 395.3 m; projection supports can extend its footprint. Use actual geometry/support, not a hardcoded correction width.

2. **Isolate truncation from resolution error.** Keep the in-domain FE phase/h unchanged. Construct the outside continuation from the same profile representation at the bottom's resolution, using nearby complete transverse sections as appropriate. Explain and check that extension. The nearby interior I_h is approximately 12692 m, whereas the analytic reference is 13054 m; do not globally substitute the analytic constant. Check that geometrically complete columns receive no virtual contribution, quantify projection effects outside the boundary layer, and verify the constant full-profile integral in the corresponding reference case.

3. **Change normalization only.** Outside data are auxiliary to normalization: add no mechanical cells, forces, or physical quadrature outside the box. Keep physical integration inside the box and use the completed I_h consistently in chi=h/I_h and all dependent coefficients. A clipped physical column may integrate chi to less than one; do not renormalize it back to unity. Leave the top endpoint, phase, fault geometry, Vp, boundary conditions, histories, and solver tolerances unchanged. Do not change the RSF transitions or prescribed-region extent.

4. **Run one short comparison.** Use the same 42880-cell mesh, 1236 vertices, mature mode, all vertices prescribed to Vp, and zero retained initial perturbation stress. Repeat initialization and exactly the baseline's two real timesteps, ending at 4970143.2669182401 s. Preserve its artificial initialization interval and production safeguards. Budget: one four-rank run, at most 10 minutes simulation wall time; no retry, refinement, or longer continuation.

Compare baseline/candidate I_h, chi, compatible-reference velocity error, and RMS |sym grad u-chi Vp S| in the existing bottom layers and interior control. Report raw bottom pressure/normal-stress extrema separately from weak averages, with correct history timing. Use the existing region definitions and analyzer.

The causal question is whether the bottom's approximately 14.1% strain mismatch approaches the same-resolution interior level of approximately 1.37%, while the raw hotspot decreases and interior changes remain small. Flattening I_h alone is insufficient evidence. Reuse existing checks; add only focused completion checks needed for this change. Deliver one compact comparison and a recommendation on the bottom normalization policy, then stop. Preserve baseline artifacts and unrelated edits.

# Preserve every physical and solver parameter; keep the baseline output separate.
include $ASPECT_SOURCE_DIR/benchmarks/reconstructed_fault/uniform_shear/diagnostics/initialization.prm
set Output directory = output-corrected

cmake_minimum_required(VERSION 3.13.4)
find_package(Aspect REQUIRED HINTS ${Aspect_DIR})
DEAL_II_INITIALIZE_CACHED_VARIABLES()
project(reconstructed_fault_uniform_shear)
add_library(uniform_shear SHARED uniform_shear.cc)
ASPECT_SETUP_PLUGIN(uniform_shear)

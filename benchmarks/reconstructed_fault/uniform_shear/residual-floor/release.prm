include $ASPECT_SOURCE_DIR/benchmarks/reconstructed_fault/uniform_shear/convergence/space16_dt05.prm
set Output directory = $ASPECT_SOURCE_DIR/benchmarks/reconstructed_fault/uniform_shear/residual-floor/release
subsection Checkpointing
  # Save after t=4 s; retain the original frozen fields across a resumed tail.
  set Steps between checkpoint = 9
end

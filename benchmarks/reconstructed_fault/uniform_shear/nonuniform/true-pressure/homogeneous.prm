# Matched true-pressure control: remove only the initial Theta bump.
include $ASPECT_SOURCE_DIR/benchmarks/reconstructed_fault/uniform_shear/nonuniform/true-pressure/pilot.prm
set Output directory = $ASPECT_SOURCE_DIR/benchmarks/reconstructed_fault/uniform_shear/nonuniform/true-pressure/homogeneous
subsection Initial composition model
  subsection Function
    set Function expression = 0; 0; 1500; 200
  end
end
